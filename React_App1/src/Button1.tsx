import * as React from "react";
const sortKeys = require('sort-keys');

const Button1 = () => {
console.log(sortKeys({c: 0, a: 0, b: 0}));
return (
<button>App 1 Remote Button</button>
)};

export default Button1;
